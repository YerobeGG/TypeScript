// In this simple assignment you are given a number and have 
// to make it negative. But maybe the number is already 
// negative

// export const makeNegative = (num: number): number => {
//     if (num > 0){return -Math.abs(num)} else {return num}
//   };